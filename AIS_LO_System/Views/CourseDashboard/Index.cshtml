@{
    ViewData["Title"] = "Course Dashboard";
}

<link rel="stylesheet" href="~/css/course.css" />
<script src="~/js/course.js" defer></script>

<div class="course-container">

    <!-- Dynamic title -->
    <h2 class="course-title">
        @ViewBag.CourseCode @ViewBag.CourseTitle
    </h2>

    <!-- Dynamic course date range -->
    <p class="course-date">
        @ViewBag.DateRange
    </p>

    <!-- Back button  -->
    <div style="margin-bottom: 20px;">
        <a class="btn btn-outline-secondary"
           asp-controller="LecturerDashboard"
           asp-action="Index"
           asp-route-year="@ViewBag.Year"
           asp-route-trimester="@ViewBag.Trimester">
            Back to Dashboard
        </a>
    </div>

    <!-- Expand / Collapse -->
    <div class="expand-toggle">
        <button id="toggleAll">Expand all</button>
    </div>

    <!-- Course Information -->
    <div class="accordion">
        <div class="accordion-header">
            <div class="header-left">
                <div class="icon-circle">
                    <span class="icon-arrow">›</span>
                </div>
                <span class="accordion-title">Course Information</span>
            </div>
        </div>

        <div class="accordion-body">
            <a asp-action="Outline">Course Outline</a>
            <a asp-action="LearningOutcomes">Learning Outcomes</a>
        </div>
    </div>

    <!-- Assessments -->
    <div class="accordion">
        <div class="accordion-header">
            <div class="header-left">
                <div class="icon-circle">
                    <span class="icon-arrow">›</span>
                </div>
                <span class="accordion-title">Assessments</span>
            </div>
        </div>

        <div class="accordion-body">
            <a asp-controller="Assignment"
               asp-action="Index"
               asp-route-id="1"
               asp-route-courseCode="@ViewBag.CourseCode"
               asp-route-courseTitle="@ViewBag.CourseTitle">
                Assignment 1
            </a>
            <a asp-controller="Assignment"
               asp-action="Index"
               asp-route-id="2"
               asp-route-courseCode="@ViewBag.CourseCode"
               asp-route-courseTitle="@ViewBag.CourseTitle">
                Assignment 2
            </a>
            <a asp-controller="Assignment"
               asp-action="Index"
               asp-route-id="3"
               asp-route-courseCode="@ViewBag.CourseCode"
               asp-route-courseTitle="@ViewBag.CourseTitle">
                Midterm
            </a>
        </div>
    </div>

    <!-- Reports -->
    <div class="accordion">
        <div class="accordion-header">
            <div class="header-left">
                <div class="icon-circle">
                    <span class="icon-arrow">›</span>
                </div>
                <span class="accordion-title">Reports</span>
            </div>
        </div>

        <div class="accordion-body">
            <a asp-action="StudentReport">Student Report</a>
            <a asp-action="CourseLOReport">Course LO Report</a>
        </div>
    </div>

</div>
