@{
    ViewData["Title"] = "Course Dashboard";
}

<link rel="stylesheet" href="~/css/course.css" />
<script src="~/js/course.js" defer></script>

<div class="course-container">

    <!-- Dynamic title -->
    <h2 class="course-title">
        @ViewBag.CourseCode @ViewBag.CourseTitle
    </h2>

    <!-- Dynamic course date range -->
    <p class="course-date">
        @ViewBag.DateRange
    </p>

    <!-- Back button  -->
    <div style="margin-bottom: 20px;">
        <a class="btn btn-outline-secondary"
           asp-controller="LecturerDashboard"
           asp-action="Index"
           asp-route-year="@ViewBag.Year"
           asp-route-trimester="@ViewBag.Trimester">
            Back to Dashboard
        </a>
    </div>

    <!-- Expand / Collapse -->
    <div class="expand-toggle">
        <button id="toggleAll">Expand all</button>
    </div>

    <!-- Course Information -->
    <div class="accordion">
        <div class="accordion-header">
            <div class="header-left">
                <div class="icon-circle">
                    <span class="icon-arrow">›</span>
                </div>
                <span class="accordion-title">Course Information</span>
            </div>
        </div>

        <div class="accordion-body">
            <a asp-controller="CourseInformation" asp-action="Outline"
               asp-route-courseCode="@ViewBag.CourseCode"
               asp-route-year="@ViewBag.Year"
               asp-route-trimester="@ViewBag.Trimester">Course Outline
           </a>

            <a asp-controller="CourseInformation" asp-action="LearningOutcomes"
               asp-route-courseCode="@ViewBag.CourseCode"
               asp-route-year="@ViewBag.Year"
               asp-route-trimester="@ViewBag.Trimester">Learning Outcomes
           </a>

        </div>

    </div>

    <!-- Assessments -->
    <div class="accordion">
        <div class="accordion-header">
            <div class="header-left">
                <div class="icon-circle">
                    <span class="icon-arrow">›</span>
                </div>
                <span class="accordion-title">Assessments</span>
            </div>
        </div>

        <div class="accordion-body">
            <a asp-action="Assessment" asp-route-id="1">Assignment 1</a>
            <a asp-action="Assessment" asp-route-id="2">Assignment 2</a>
            <a asp-action="Assessment" asp-route-id="3">Midterm</a>
        </div>
    </div>

    <!-- Reports -->
    <div class="accordion">
        <div class="accordion-header">
            <div class="header-left">
                <div class="icon-circle">
                    <span class="icon-arrow">›</span>
                </div>
                <span class="accordion-title">Reports</span>
            </div>
        </div>

        <div class="accordion-body">
            <a asp-action="StudentReport">Student Report</a>
            <a asp-action="CourseLOReport">Course LO Report</a>
        </div>
    </div>

</div>
